#ifndef VARIABLE_H_INCLUDED
#define VARIABLE_H_INCLUDED


#include "stdio.h"
#include "stdlib.h"
#include "iostream"
#include "string.h"
#include "math.h"


typedef struct student	Stu,	*pStu;
typedef struct link 	Link,	*pLink;
typedef struct bstree 	BSTree,	*pBSTree;
typedef struct STACK 	Stack,	*pStack;
typedef struct operate 	Operate,*pOperate;
typedef struct cmm 		Cmm,	*pCmm;

#include "variable.cpp"

#endif // VARIABLE_H_INCLUDED
